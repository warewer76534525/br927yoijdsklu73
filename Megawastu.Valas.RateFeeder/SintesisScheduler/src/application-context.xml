<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans	
		http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://www.springframework.org/schema/context 
		http://www.springframework.org/schema/context/spring-context.xsd 
		http://www.springframework.org/schema/tx 
        http://www.springframework.org/schema/tx/spring-tx-3.0.xsd
">

	<!-- Basic Configuration -->
	<tx:annotation-driven />
	<context:component-scan base-package="com.triplelands.megawastu.valas.moneychanger" />
	<context:property-placeholder location="application.properties" />
	<context:annotation-config />

	<!-- JMS Configuration -->
	<bean id="connectionFactory" class="org.apache.activemq.ActiveMQConnectionFactory">
		<property name="brokerURL" value="${jms.brokerURL}" />
	</bean>

	<bean class="org.springframework.jms.listener.DefaultMessageListenerContainer">
		<property name="connectionFactory" ref="connectionFactory" />
		<property name="destinationName" value="${jms.ratesUpdate.topic}" />
		<property name="messageListener" ref="rateUpdatedListener" />
	</bean>

	<bean id="sintesisJmsTemplate" class="org.springframework.jms.core.JmsTemplate">
		<property name="connectionFactory" ref="connectionFactory" />
		<property name="receiveTimeout" value="10000" />
		<property name="defaultDestinationName" value="${jms.fixRate.topic}" />
		<property name="messageConverter" ref="ratesMessageConverter" />
	</bean>


	<bean id="sintesisService"
		class="com.triplelands.megawastu.valas.moneychanger.sintesis.service.SintesisService">
		<property name="currencyListForSintesis">
			<list>
				<value>IDR</value>
				<value>MYR</value>
			</list>
		</property>
	</bean>

	<!-- Scheduler Config -->

	<bean name="sintesisGeneratorJob" class="org.springframework.scheduling.quartz.JobDetailBean">
		<property name="jobClass"
			value="com.triplelands.megawastu.valas.moneychanger.sintesis.scheduler.SintesisGeneratorJob" />

		<property name="jobDataAsMap">
			<map>
				<entry key="sintesisService" value-ref="sintesisService" />
			</map>
		</property>

	</bean>

	<bean id="simpleTrigger" class="org.springframework.scheduling.quartz.SimpleTriggerBean">
		<property name="jobDetail" ref="sintesisGeneratorJob" />
		<property name="repeatInterval" value="${quartz.sintesis.interval}" />
		<property name="startDelay" value="1000" />
	</bean>

	<bean class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
		<property name="jobDetails">
			<list>
				<ref bean="sintesisGeneratorJob" />
			</list>
		</property>

		<property name="triggers">
			<list>
				<ref bean="simpleTrigger" />
			</list>
		</property>
	</bean>

</beans>


